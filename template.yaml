apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: nextjs-starter
  title: NextJS Starter Template
spec:
  owner: platform-team
  type: service
```

### **Option 2: Backstage structure (recommended if using `fetch:template`)**
```
nextjs-starter-temp/
├── template.yaml          ← Metadata file
└── skeleton/              ← All your actual files go here
    ├── package.json
    ├── pages/
    ├── Dockerfile
    ├── k8s/
    └── .harness/
